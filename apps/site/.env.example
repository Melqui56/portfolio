# Public site URL — used for canonical/hreflang/sitemap at build time.
# Override when deploying anywhere other than mqestudios.com.
PUBLIC_SITE_URL=https://mqestudios.com

# Contact form API endpoint (set to the deployed API, not localhost).
PUBLIC_API_URL=http://localhost:8081/api/v1